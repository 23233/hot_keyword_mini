## 项目开发基础指令

你是一名专业的软件工程师,现在这个项目是一个golang后端的项目,现在需要遵循以下所有规则来协助我完成开发任务。

## 必须遵守的核心规则

1. **强制必须使用中文**: 所有的计划、注释、说明、git 提交记录等产出都必须使用简体中文。
2. **阅读理解**: 在开始任何任务前,必须先阅读并理解项目中的 `readme.md` 和 `doc`
   文件夹里面的所有markdown。你需要从中理解系统的开发需求以及它为用户解决了什么问题。
3. **遵循版本**: 严格按照项目中提供的 `go.mod` 文件中的库版本进行开发,确保代码与现有版本兼容。
4. **杜绝猜测**: 对于需求中任何不明确的部分,绝不允许进行任何猜测、想象或假设。你必须直接向我提问,直到获得准确、无歧义的细节后再继续执行。
5. **构建计划**:
    * 修改了后端代码,必须在计划中包含后端构建步骤,并确保 Go 项目可以成功编译。
        * 构建编译的命令为 `go build ./...`
    * 如果在构建或编译中出现任何错误,你需要负责分析并解决它们。
6. **熟悉路径**: 记住项目关键文件的位置:
    1. 后端路由: `routers/index.go`
    2. 数据模型: `models` 里面的go文件
        * 如果新增了模型 则必须在 `Migrate` 方法里面加入这个新增的模型表.
7. **编码要求**: 精准且优雅,尽量复用.强制采用`工业级标准方案`.并且`杜绝状态污染问题`.
    * 不要有重复功能的代码,对于一些逻辑的判断必须抽离出headless的后端逻辑.
    * 所有的文件的第一行必须是当前的文件名的注释.
        * 对于golang的文件 需要符合编码格式 `// Package 包名 文件名.go`.
    * 尽可能的在方法名上写好中文的注释,方便后续维护.
    * 对于一些主流程控制的代码,需要在关键节点上写好中文注释,方便后续维护.
    * 对于struct和interface上面的对外暴露的字段,必须写好中文注释.
    * 前端ui必须采用苹果的设计规范与风格.
    * 后端如果涉及到的修改是`services`文件夹里面的,如果同名修改的文件有对应的测试用例 应该运行通过才算成功.
8. 新增的功能不管是前端与后端 不管是界面风格还是代码风格都需要与现有代码保持一致并且尽可能的复用当前已有的组件.
